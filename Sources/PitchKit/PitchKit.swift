/// PitchKit — absolute pitch + pitch class (no key / chord / UI).
///
/// - ``Pitch`` — MIDI identity; octave and class derived
/// - ``PitchClass`` — 12-TET class with sharp/flat names
public enum PitchKit {
    public static let version = "0.1.0"
}
